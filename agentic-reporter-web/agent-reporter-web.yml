agent:
  name: agent-reporter-web  
  title: "Agent Reporter Web"
  description: "Agent to produce web content for sportsblog"
  metadata:
    language: "en-us"
  context:
    config-frequency: 1
  workflows:
    - name: "workflow-reporter-web"
      description: "Reporter web workflow"
      outputs:
        reporter-web: "$.get('events')"

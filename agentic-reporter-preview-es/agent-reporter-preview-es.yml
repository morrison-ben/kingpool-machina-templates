agent:
  name: "agent-reporter-preview-es"
  title: "Blog Reporter Previa del Partido"
  description: "Este agente genera contenido en español para previas de partidos."
  metadata:
    language: "es"
  context:
    config-frequency: 1
  workflows:
    - name: "workflow-reporter-preview-es"
      description: "Reporter preview workflow"
      outputs:
        preview: "$.get('events')"

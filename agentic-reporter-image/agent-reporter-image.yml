agent:
  name: agent-reporter-image  
  title: "Agent Reporter image"
  description: "Agent to produce content image for sportsblog"
  metadata:
    language: "en-us"
  context:
    config-frequency: 1
  workflows:
    - name: "workflow-image-preview"
      description: "Create image preview"
      outputs:
        image_preview: "$.get('events')"